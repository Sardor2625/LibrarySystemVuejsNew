// api.js (Utility file for handling HTTP requests)

import axios from 'axios';

// Set up default base URL for API requests
axios.defaults.baseURL = 'https://your-api-url.com';

// Set up Axios interceptors, headers, or other configurations if needed
// For example, adding authorization headers, intercepting requests, etc.

export default axios;
